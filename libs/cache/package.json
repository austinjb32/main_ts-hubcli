{
  "name": "cache",
  "version": "1.0.0",
  "description": "cache",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "rimraf dist && node ./esbuild.config.js && tsc --emitDeclarationOnly --outDir dist"
  }
}
